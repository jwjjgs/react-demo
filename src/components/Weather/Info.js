import React, { useState, useEffect } from "react";
import { Layout } from "antd";

export default (props)=>{
    const {} = props;

    return (
        <Layout style={{minHeight:300}}>
            <Layout.Footer>dada</Layout.Footer>
        </Layout>
    )
}